server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stock
    username: user
    password: user_password

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

appDescription: "Category API"
appVersion: "1.0.0"